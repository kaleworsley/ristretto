  <% flash[:error] = form.error_messages %>
  <% if @stakeholder.user.blank? %>
    <%= form.collection_select :user_id, @valid_users, :id, :full_name %>
  <% else %>
    <%= hidden_field :user_id, @stakeholder.user.id %>
    <%= link_to @stakeholder.user.full_name, @stakeholder.user %> -
  <% end %>
  <%= form.select :role, Stakeholder.roles_for_select %>
  <%= form.submit "Save" %>
