<%- content_for :breadcrumbs do -%>
  <%= crumb @project.customer.name, @project.customer %>
  <%= crumb @project.name, @project %>
  <%= crumb 'New stakeholder', new_project_stakeholder_path(@project) %>
<%- end -%>

<%- content_for :tabs do -%>
  <%= tab 'Stakeholders', project_stakeholders_path(@project) if can? :index, Stakeholder %>
<%- end -%>

<%= title "New stakeholder" -%>

<% form_for [@project, @stakeholder] do |form| %>
  <%= render form %>
<% end %>
