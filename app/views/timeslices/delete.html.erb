<%- content_for :tabs do -%>
    <%= tab 'Edit', edit_timeslice_path(@timeslice) %>
<%- end -%>

<%= title "Are you sure you want to continue?" -%>
This will delete timeslice #<%=@timeslice.id %>.
<% form_for @timeslice, :html => {:method => 'delete'} do |f| %>
  <%= submit_tag 'Delete' %> or <%= link_to 'Cancel', request.referer || @timeslice, {:class => 'cancel'} %>
<% end %>