<%= @comment.user %> has posted a comment on task "<%= @comment.task %>" for project "<%= @comment.task.project %>".
<% unless @user.is_staff? %>
You can login to Ristretto and view the comment by visiting the task below.
<% else %>

<%= @comment.body %>

Read more:
<% end %>
  <%= task_url(@comment.task) %>#comment-<%= @comment.id %>

---
To stop notification for this task or project, please log in to <%= SETTINGS['name'] %> and visit on of the following links and click disable notifications.

Project: <%= project_url(@comment.task.project) %>
Task:    <%= task_url(@comment.task) %>